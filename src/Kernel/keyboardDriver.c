#include <keyboardDriver.h>

/* funcion a la que llama la interrupcion del teclado
	getKey le da la tecla pero falta todo lo demas 
	que no tengo idea
*/
void keyboardHandler() {

	int key = _getKey();
	
}